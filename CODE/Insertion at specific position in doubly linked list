def insert_at_specific_position(self,position,data):
    print()
    nib=Node(data)
    temp=self.head
    for i in range(1,position-1):
    temp=temp.next
    nib.prev=temp
    temp.next.prev=nib
    temp.next=nib
  
LL.insert_at_specific_position()
