STACK:

1) STACK- It's a memory position which is used for sorting
          elements in LIFO order
          
          
2) TOP- The top pointer points the top elements of the stack
         from where elements are to be inserted or deleted
         
         
3) UNDERFLOW- When there is no element in the stack or
        stack elements less than its capacity the stack is said to be underflow
        
        
4) OVERFLOW- When the stackcontains no of elements equal
             to its capacity and no more elements be added then its called as overflow
